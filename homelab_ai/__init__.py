"""HomeLab AI — terminal AI companion.

The package is split into a *brain* (LLM logic + tools, preserved from the
original ``agent-dev.py``) and a *CLI/TUI* layer rebuilt on top of Textual,
pydantic-ai, LangGraph, pexpect, plumbum and watchfiles.
"""

__version__ = "2.0.0"
